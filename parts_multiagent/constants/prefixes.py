"""User query prefixes for parts multi-agent routing."""

LOCAL_AGENT_PREFIX = '/local'
PEER_AGENTS_PREFIX = '/peers'
USER_QUERY_PREFIXES = (LOCAL_AGENT_PREFIX, PEER_AGENTS_PREFIX)

LOCAL_STOCK_INBOUND_PREFIX = 'local-stock-inbound'
LOCAL_STOCK_OUTBOUND_PREFIX = 'local-stock-outbound'
PEER_STOCK_INBOUND_PREFIX = 'peer-stock-inbound'
PEER_STOCK_OUTBOUND_PREFIX = 'peer-stock-outbound'
LOCAL_INVENTORY_QUERY_PREFIX = 'local-inventory-query'
PEER_INVENTORY_QUERY_PREFIX = 'peer-inventory-query'
